"""eval — metrics and golden test sets.

Ranking metrics (top-k accuracy, MRR on differential lists) plus synthetic,
hand-constructed golden vignettes with known-correct rankings. Golden sets are
synthetic by design: this repository never contains real patient data.
"""
