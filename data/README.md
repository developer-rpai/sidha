# data/

**Synthetic or sample data ONLY.** Never commit real patient data, scraped
datasets with unclear licensing, or anything covered by a data-use agreement.

- `synthetic/` — generated fixtures for tests and demos (safe to commit)
- `samples/` — tiny hand-built examples with provenance notes (safe to commit)
- `raw/`, `processed/` — local working directories, git-ignored by default
